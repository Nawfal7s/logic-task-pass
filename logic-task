#1: method to remove and given character from a string.
# using replace() method

#CODE:

#removes all accurances of "e"
str = "software engineering"
new_str = str.replace('e', '')
print(new_str)
#OUTPUT:
softwar nginring


#2: A program to find all prime numbers up to a given range of numbers.


#CODE:

lower = 1
upper = 20

for num in range(lower, upper + 1):
   if num > 1:
      for i in range(2, num):
         if (num % i) == 0:
            break
      else:
          print(num)
         
#OUTPUT:
2
3
5
7
11
13
17
19


#3: A function to count how many a given character is repeated in a given string.

#CODE:

def count_func():
    strr = "software engineer"
    how_many_EEEs = strr.count('e')
    print(how_many_EEEs)

count_func()

#OUTPUT:
4
